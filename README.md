# jli releases

Public macOS binaries and installer for `jli`.

```sh
curl -LsSf https://github.com/kyosterm/jli-releases/releases/latest/download/install.sh | sh
```

Run the same command again to upgrade.
